spring:
  application:
    name: system
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://8.136.215.55:3306/booking_system?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 111111
server:
  port: 8080
  mybatis-plus:
    # 2. ?????? (????)
    type-aliases-package: com.tomorrowmust.system.domain
    configuration:
      map-underscore-to-camel-case: true
      # 4. ?? SQL ?? (???????????)
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    global-config:
      db-config:
        logic-delete-field: isDeleted
        logic-delete-value: 1
        logic-not-delete-value: 0
        id-type: ASSIGN_ID
  data:
    redis:
      port: 6379
      host: 8.136.215.55
      lettuce:
        pool:
          max-active: 10
          max-idle: 10
          min-idle: 1
          time-between-eviction-runs: 10s
springdoc:
  api-docs:
    enabled: true # ?? API ????
    path: /v3/api-docs
  swagger-ui:
    enabled: true # ?? Swagger UI
    path: /swagger-ui.html

knife4j:
  enable: true # ?? Knife4j ??
  setting:
    language: zh_cn # ???????
    swagger-model-name: ????? # ????
    enable-filter-multipart-apis: true # ?? multipart ??
    enable-filter-multipart-api-method-type: POST